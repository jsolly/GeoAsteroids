{
  "install": "npm ci || npm install && (test -f .env || cp .env.example .env)",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev"
    }
  ],
  "agentCanUpdateSnapshot": true
}
